#!/bin/bash

# Run from same dir of your .bashrc file.

# If no .bash_aliases is existent, add this to .bashrc
# if [ -f ~/.bash_aliases ]; then
#      . ~/.bash_aliases
# fi

cat <<EOT >> .bash_aliases
alias c='clear'
alias d='cd ../'
alias e='exit'
EOT

source .bashrc
exit;